<!DOCTYPE html>
<html lang="es">

<head>
    <%- include("../partials/head.ejs") %>
        <link href="/css/modify.css" rel="stylesheet">
</head>

<body>
    <%- include("../partials/header.ejs") %>

        <main>
            <form enctype="multipart/form-data" action="/products/<%= producto.id %>?_method=PUT" method="post" class="frmCarga">
                <section class="carga">
                    <section class="informacionProducto">
                        <h3>Nombre</h3>
                        <input type="text" name="nombre" id="nombre" class="campos" value="<%= producto.nombre %>">

                        <h3>Descripcion</h3>
                        <input type="text" name="descripcion" id="descripcion" class="descr" value="<%= producto.descripcion %>">

                        <h3>Categoria</h3>
                        <select name="categoria" class="campos" value="<%= producto.categoria %>" required>
                
                    <option value="1">Paletas</option>
                    <option value="2">Pelotas</option>
                    <option value="3">Bolsos</option>
                    <option value="4">Pistas</option>

                </select>

                        <h3>Precio</h3>
                        <input type="text" name="precio" id="precio" class="campos" value="<%= producto.precio %>">

                        <h3>Stock</h3>
                        <input type="text" name="stock" id="stock" class="campos" value="<%= producto.stock %>">
                    </section>
                    <section class="cargaImagen">
                        <img src="<%= producto.imagen %>" alt="">
                        <div class="file-select" id="src-file1">
                            <input type="file" name="imagen" aria-label="Archivo">
                        </div>
                    </section>

                    <section class="botones">

                        <button type="submit" class="boton guardar">Guardar Cambios</a></button>
            </form>
            <form action="/products/<%= producto.id %>?_method=DELETE" method="post" class="btnEliminar">
                <button type="submit" class="boton">Eliminar Producto</button>
            </form>


            </section>

            </section>

        </main>

        <%- include("../partials/footer.ejs") %>
</body>

</html>